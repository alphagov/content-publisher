{
  "name": "content-publisher",
  "scripts": {
    "postdeploy": "rake db:migrate db:seed"
  },
  "env": {
    "GDS_SSO_STRATEGY": {
      "required": true
    },
    "GOVUK_APP_DOMAIN": {
      "required": true
    },
    "JWT_AUTH_SECRET": {
      "generator": "secret"
    }
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/ruby"
    }
  ]
}
