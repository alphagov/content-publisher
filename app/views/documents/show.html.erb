<% content_for :back_link, documents_path %>
<% content_for :title, @document.title || t("documents.show.title_default") %>
<% content_for :context, @document.document_type_schema.label %>

<%= render "govuk_publishing_components/components/tabs", {
  panel_border: false,
  tabs: [
    {
      id: "edit-document",
      label: "Edit document",
      content: render("documents/show/summary_tab"),
    },
    {
      id: "document-history",
      label: "Document history",
      content: render("documents/show/history_tab"),
    }
  ]
} %>

<%= render "documents/show/internal_metadata" %>
