<div id=<%= name %>>
  <p><%= name %>:</p>

  <% linkables = LinkablesService.new("world_location") %>
  <% if value&.any? %>
    <ul>
      <% value.map do |field| %>
        <li><%= linkables.by_content_id(field)["title"] %></li>
      <% end %>
    </ul>
  <% else %>
    None
  <% end %>
</div>
