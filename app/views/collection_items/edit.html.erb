<% content_for :back_link, render_back_link(href: collection_items_path(@edition.document, @collection.id)) %>
<% content_for :title, "Edit #{@item[@collection.distinguishing_field]}" %>

<%= form_tag(
  update_collection_item_path(@edition.document, @collection.id, @item["id"]),
  method: :patch,
  html: { class: "app-c-contextual-guidance__form" },
  data: { "warn-before-unload": "true" }
) do %>

  <% @collection.fields.each do |field| %>
    <%= render "content/edit/#{field.id}_field", field: field, edition: @edition, data: @item, issues: @issues %>
  <% end %>

  <%= render "govuk_publishing_components/components/button", {
    text: "Save", margin_bottom: true
  } %>
<% end %>
