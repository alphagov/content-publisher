<% content_for :back_link, document_path(@document) %>
<% content_for :title, t("pages.publish_document.confirmation.title") %>

<%= form_tag publish_document_path(@document) do %>
  <%= render "govuk_publishing_components/components/button", {
    text: t("pages.publish_document.confirmation.actions.confirm")
  } %>
<% end %>
